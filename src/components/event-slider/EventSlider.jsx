import React from "react";
import styled from "styled-components";

const Styled = styled.div`
  background: blueviolet;
  min-height: 400px;
`;
const EventSlider = () => {
  return <Styled>EventSlider</Styled>;
};

export default EventSlider;
